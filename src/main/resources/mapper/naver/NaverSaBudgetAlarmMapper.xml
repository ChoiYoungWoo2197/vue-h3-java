<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.h3.h3_java.media.naver.mapper.NaverSaBudgetAlarmMapper">

    <select id="selectAlarmSetting" resultType="map">
        SELECT type, im, clk, cv, cr
        FROM h3_budget_daily_alarm
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <!-- 비즈머니 잠금 알람: 최근 1일 중복 체크 -->
    <select id="countRecentBizmoneyAlarm" resultType="int">
        SELECT COUNT(*)
        FROM h3_budget_alarm
        WHERE user_id    = #{userId}
          AND daily_advid = #{advid}
          AND daily_dt    BETWEEN DATE_ADD(NOW(), INTERVAL -1 DAY) AND NOW()
          AND media       = 'naver'
          AND type        = 'bizmoney_locked_by_budget'
    </select>

    <!-- 계정/캠페인 KPI 인상률 알람: 최근 7일 중복 체크 -->
    <select id="countRecentAlarm" resultType="int">
        SELECT COUNT(*)
        FROM h3_budget_alarm
        WHERE user_id    = #{userId}
          AND daily_advid = #{advid}
          AND daily_dt    BETWEEN DATE_ADD(NOW(), INTERVAL -7 DAY) AND NOW()
          AND media       = 'naver'
          AND level       = #{level}
          AND type        = #{type}
        <if test="targetId != null">
          AND target_id = #{targetId}
        </if>
    </select>

    <insert id="insertBudgetAlarm" parameterType="map">
        INSERT INTO h3_budget_alarm (
            user_id, daily_advid, daily_dt,
            media, target_id, target_name,
            level, type, content, daily_regdate
        ) VALUES (
            #{user_id}, #{daily_advid}, #{daily_dt},
            #{media}, #{target_id}, #{target_name},
            #{level}, #{type}, #{content}, #{daily_regdate}
        )
    </insert>

</mapper>
