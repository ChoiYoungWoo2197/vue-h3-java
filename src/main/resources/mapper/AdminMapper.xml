<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.h3.h3_java.api.mapper.AdminMapper">

    <!-- 이관용: h3_share 전체 읽기 -->
    <select id="selectAllShare" resultType="map">
        SELECT user_id, user_manager, share_manager, share_favorites, share_regdate
        FROM h3_share
    </select>

    <!-- 이관용: h3_adv 전체 읽기 -->
    <select id="selectAllAdv" resultType="map">
        SELECT user_id, adv_id, adv_media, adv_regdate, adv_marketer, adv_marketer_regdate, adv_salse_day
        FROM h3_adv
    </select>

    <!-- 매체별 일별 비용 합계 (tableName: h3_campaign_daily_naver 등, 화이트리스트로 제어) -->
    <select id="selectDailyCstByTable" resultType="map">
        SELECT IFNULL(SUM(daily_cst), 0) AS cst, daily_advid
        FROM ${tableName}
        WHERE daily_dt BETWEEN #{fromDate} AND #{toDate}
        GROUP BY daily_advid
    </select>

    <!-- 통합 알림: admin 전체 -->
    <select id="selectAlarmForAdmin" resultType="map">
        SELECT *
        FROM h3_budget_alarm
        ORDER BY daily_dt DESC
        LIMIT #{start}, #{display}
    </select>

    <select id="countAlarmForAdmin" resultType="long">
        SELECT COUNT(*) FROM h3_budget_alarm
    </select>

    <!-- 통합 알림: marketer (advId IN 필터) -->
    <select id="selectAlarmForMarketer" resultType="map">
        SELECT *
        FROM h3_budget_alarm
        WHERE daily_advid IN
        <foreach collection="advIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY daily_dt DESC
        LIMIT #{start}, #{display}
    </select>

    <select id="countAlarmForMarketer" resultType="long">
        SELECT COUNT(*)
        FROM h3_budget_alarm
        WHERE daily_advid IN
        <foreach collection="advIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>
