<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.h3.h3_java.api.mapper.AccountMapper">

    <select id="selectByUserId" resultType="com.h3.h3_java.api.dto.AccountDto">
        SELECT
            user_id,
            account_naver_access,
            account_naver_secret,
            account_naver_customer,
            account_kakaosa,
            account_kakaomoment,
            account_gfa,
            account_google
        FROM h3_account
        WHERE user_id = #{userId}
    </select>

    <!-- 배너 타입: advkey로 실제 계정 소유자 user_id 조회 -->
    <select id="selectUserIdByGfa" resultType="string">
        SELECT user_id FROM h3_account WHERE account_gfa = #{advid} LIMIT 1
    </select>

    <select id="selectUserIdByKakaomo" resultType="string">
        SELECT user_id FROM h3_account WHERE account_kakaomoment = #{advid} LIMIT 1
    </select>

    <select id="selectUserIdByGoogle" resultType="string">
        SELECT user_id FROM h3_account WHERE account_google = #{advid} LIMIT 1
    </select>

    <!-- 전체 이관용 -->
    <select id="selectAll" resultType="map">
        SELECT user_id, account_naver, account_naver_customer,
               account_naver_access, account_naver_secret,
               account_gfa, account_kakaosa, account_kakaomoment,
               account_google, account_date, account_regdate
        FROM h3_account
        ORDER BY user_id
    </select>

</mapper>
